name: Publicar minha documentação

on:
  push:
    branches:
      - main # Executa a automação toda vez que você der 'git push' na branch main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # necessário para git-revision-date

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Instalar dependências
        run: pip install -r requirements.txt

      - name: Publicar no GitHub Pages
        run: mkdocs gh-deploy --force
